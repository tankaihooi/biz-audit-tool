# --- App runtime ---
fastapi>=0.111
uvicorn[standard]>=0.30
pydantic>=2.7
python-dotenv>=1.0

# --- Frontend ---
streamlit>=1.35
requests>=2.32

# --- RAG ---
# CPU-only torch wheels are a fraction of the size of the default CUDA build -
# keeps builds fast and fits free-tier deploy resource limits.
--extra-index-url https://download.pytorch.org/whl/cpu
chromadb>=0.5
sentence-transformers>=3.0

# --- LLM ---
google-genai>=1.0
